apiVersion: apps/v1
kind: Deployment
metadata:
  name: app2-deployment
  labels:
    app: app2

spec:
  replicas: 3
  selector:
    matchLabels:
      app: app2

  template:
    metadata:
      labels:
        app: app2
    spec:
      containers:
      - name: app2-container
        image: docker.io/paulbouwer/hello-kubernetes
        ports:
        - containerPort: 80  # port inside the container (must match the app's exposed port)

        resources:
          limits:
            cpu: "500m"  # max 0.5 CPU cores
            memory: "256Mi"  # max 256MB memory
          requests:
            cpu: "250m"  # request 0.25 CPU cores initially
            memory: "128Mi"  # request 128MB memory initially

      restartPolicy: Always

        # env:
        # - name: NODE_ENV
        #   value: "production"

        # volumeMounts:
        # - name: app2-storage
        #   mountPath: "/app/data"

      # volumes:
      # - name: app2-storage
      #   emptyDir: {}

